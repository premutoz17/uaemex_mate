//Valor logico and, or, not
#include<iostream>

using namespace std;

int main (){

//El valor logico de and es &&, el de or es ||, el de not ! (se pone antes del parentesis).

    int a=1, b=3, c=2, d=7;
    bool comprobar=!(b>a);

    cout<<comprobar<<endl;

    return 0;
}
