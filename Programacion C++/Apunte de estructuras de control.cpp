/*Estructuras de decision*/

#include<stdio.h>

int main(){

    printf("Las estructuras de control como las de desicion, repetitivas y estructuras de seleccion multiple permiten transferir el control a otras estructuras sin seguir
           "la ejecucion secuencial. Las estructuras selectivas se uutilizan para tomar desiciones logicas. La estructura switch case, es una estructura de desicioin multiple,
           "donde el compliador prueba o busca el valor contenido en una variable contra una lista de constantes tipo entero o char.
           "Si dicha variable no existe se ejecuta la instruccion por default.
           "Una cadena esta definida como arreglo de caracteres asii. Las cadenas de caracteres o strings son un tipo particular de vector de tipo char
           "que tiene una marca de fin caracter NULL o \0 que indica que la cadena ha terminado.");

return 0;
}
